[package]
name = "inf-water"
description = "Water surfaces: Gerstner wave math, river frames, shore queries (pure f64, bit-portable)."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish = false

[dependencies]
glam = { workspace = true }
# The bit-portable trig (`psin64`/`pcos64`) every wave phase is built from, and
# the spline arc-length machinery a river centreline rides. Ring-0 → Ring-0;
# `inf-math` names nothing, so there is no cycle.
inf-math = { workspace = true }
