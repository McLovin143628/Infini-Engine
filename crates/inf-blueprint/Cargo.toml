[package]
name = "inf-blueprint"
description = "Infinity Blueprints: graph semantics + tree-walking interpreter for editor preview."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish = false

[dependencies]
serde = { workspace = true }
thiserror = { workspace = true }
inf-graph = { workspace = true }
# Portable trig (psin64/pcos64) — `math.sin`/`math.cos` route here so committed
# blueprint math stays bit-identical across platforms (std trig is not; see
# inf_math::portable house law).
inf-math = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }
