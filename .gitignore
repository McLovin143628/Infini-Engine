# ── Claude / AI assistant local files (never push) ──
CLAUDE.md
**/CLAUDE.md
.claude/
.claude/**

# ── Secrets / credentials / personal info (never push) ──
.env
.env.*
*.pem
*.key
*.p12
*.pfx
*credentials*
*secrets*
*.local.json
settings.local.*

# ── The island's heavy halves (wave I7) ──
# `inf island build` writes its terrain, road mesh and tile cache OUTSIDE the
# tree by default (`<checkout>/../island-build/`), which is the whole point of a
# recipe. These two entries are belt-and-braces for an author who points `--out`
# or `[source] cache` back inside it — a third of a gigabyte of build artifact
# in a source tree is not something to find out about from a push.
island-build/
samples/island/build/

# ── Rust ──
target/
target-*/
Cargo.lock.orig
**/*.rs.bk

# ── Node / frontend ──
node_modules/
dist/
dist-ssr/
.vite/
*.tsbuildinfo

# ── Tauri generated ──
**/src-tauri/gen/
**/gen/schemas/

# ── Editors / OS junk ──
.DS_Store
Thumbs.db
Desktop.ini
*.swp
*.swo
.idea/
.vscode/*
!.vscode/extensions.json

# ── Docs (mdBook rendered output) ──
docs/book/book/

# ── Logs / temp ──
*.log
build_log*.txt
tmp/
temp/
